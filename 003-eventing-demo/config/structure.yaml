---
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: structure
  namespace: default
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/maxScale: "1"
        autoscaling.knative.dev/minScale: "1"
    spec:
      containers:
        - image: ko://kndemo/cmd/structure
          env:
          - name: LLM_API_BASE_URL
            value: http://host.docker.internal:11434/v1
---
apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: structure
  namespace: default
spec:
  broker: default
  filter:
    attributes:
      type: intake.new
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: structure
